<h1>User info</h1>
<% if @user.errors.any? %>
     <h3><%= pluralize(@user.errors.count, "error") %> prohibited this book from being saved:</h3>
     <ul><% @user.errors.full_messages.each do |message| %>
     	 <li><%= message %></li>
         <% end %>
     </ul>
<% end %>

<%= form_for(@user) do |f| %>
<h4>Name</h4>
<%= f.text_field :name %>
<h4>Image</h4>
<%= attachment_image_tag @user, :profile_image, fallback: "no_image.jpg" %>
<%= f.attachment_field :profile_image, placeholder: "プロフィール画像" %>
<h4>Introduction</h4>
<%= f.text_area :introduction %>
<br><button class="btn btn-primary">Update User</button></br>
<% end %>