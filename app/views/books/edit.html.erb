<div class="container"> 
<div class="row">
<h1>Editing Book</h1>

<% if @book.errors.any? %>
     <h3><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h3>
     <ul><% @book.errors.full_messages.each do |message| %>
     	 <li><%= message %></li>
         <% end %>
     </ul>
<% end %>

<%= form_for(@book) do |f| %>
<h4>Title</h4>
    <%= f.text_field :title %>
<h4>Opinion</h4>
    <%= f.text_area :body %>
    <br><button class="btn btn-primary">Update Book</button></br>
<% end %>

<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back", books_path %>
</div>
</div>
</div>